import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
	reactStrictMode: false, // Отключаем Strict Mode для разработки
 };

export default nextConfig;
